services:
  app:
    image: nginx:latest
    ports:
    - 8080:80
    healthcheck:
      test:
      - curl
      - -f
      - http://localhost:80
      interval: 30s
      timeout: 10s
      retries: 3
