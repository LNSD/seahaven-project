[package]
name = "seahaven-file-testdata"
version = "0.0.0"
edition.workspace = true
publish = false

[features]
codegen = []

[dependencies]
indoc = "2"

[build-dependencies]
minijinja = { version = "2", features = ["builtins"] }
