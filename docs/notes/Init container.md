Raw notes for [[docs-drafts/Init container]] concept.
# Notes
- Init containers should have something similar to `healthchecks`, a simple check script or similar that validates the correct execution of the init task